#include "main.h"
#include <stdio.h>

/**
 * add - add two numbers together
 * @a: one argument
 * @b: another argument to be added
 * Return: returns 0 if successful
 */

int add(int a, int b)

{
	return (a + b);

}
